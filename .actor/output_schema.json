{
    "actorOutputSchemaVersion": 1,
    "title": "Grant Finder Results",
    "type": "object",
    "properties": {
        "grantName": {
            "type": "string",
            "title": "Grant Name",
            "template": "{{value}}"
        },
        "eligibilityFocus": {
            "type": "string",
            "title": "Eligibility",
            "template": "{{value}}"
        },
        "fundingTier": {
            "type": "string",
            "title": "Funding Tier",
            "template": "{{value}}"
        },
        "complexity": {
            "type": "string",
            "title": "Complexity",
            "template": "{{value}}"
        },
        "riskAudit": {
            "type": "string",
            "title": "Risk Level",
            "template": "{{value}}"
        },
        "directLink": {
            "type": "string",
            "title": "Link",
            "template": "{{value}}"
        },
        "professionalPitch": {
            "type": "string",
            "title": "Pitch",
            "template": "{{value}}"
        }
    }
}
